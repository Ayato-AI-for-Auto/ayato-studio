# Ayato Studio LP (Landing Page)

このレポジトリは、AIエンジニア「あやと」のブランドハブとなる `ayato-studio.ai` のソースコード一式だ。
「無駄な作業」を徹底的に排除し、AIが自動でコンテンツを生成・デプロイする仕組みを構築している。

## テックスタック
- **Hosting**: GitHub Pages (完全無料・停止リスクなし)
- **Domain**: `ayato-studio.ai` (Cloudflare DNS管理)
- **Automation**: GitHub Actionsを使わず、ローカルの `auto_deploy.py` で一撃デプロイ。

## デプロイ方法
「手動でプッシュ」なんて時間の無駄だ。以下のスクリプトを叩け。
```powershell
python product/agent_server/scripts/auto_deploy.py
```
- **ロジック**: フォルダのハッシュを計算し、変更がある場合のみ GitHub へプッシュする。

## コンテンツ管理
### 設定 (`config.js`)
URLsやSNSリンクはすべてここで一括管理している。
```javascript
const CONFIG = {
    X_URL: "https://x.com/TechLong_Invest",
    // 変更が必要な場合はここだけ修正しろ
};
```

### ブログ更新 (`migrate_blog.py`)
`agent_server` 側のスクリプトで、過去の記事や新着トレンドを自動でHTML化し、このLPにインジェクションする。

## ディレクトリ構造
- `index.html`: 核となるシングルページLP。
- `blog/`: 自動生成された技術記事。
- `config.js`: 動的設定ファイル。
- `style.css`: グラスモーフィズムを採用したプレミアムデザイン。
- `script.js`: インタラクション制御。
- `CNAME`: GitHub Pages用のカスタムドメイン設定。

---
*Generated by Antigravity Logic Engine*
